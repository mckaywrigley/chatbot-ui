# # apiVersion: v1
# # kind: Namespace
# # metadata:
# #   name: chatbot-ui
# # ---
# # apiVersion: v1
# # kind: Secret
# # metadata:
# #   namespace: chatbot-ui
# #   name: chatbot-ui
# # type: Opaque
# # data:
# #   OPENAI_API_KEY: <OPEN-API-KEYS>
# ---
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   namespace: chatbot-ui
#   name: chatbot-ui
#   labels:
#     app: chatbot-ui
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: chatbot-ui
#   template:
#     metadata:
#       labels:
#         app: chatbot-ui
#     spec:
#       containers:
#         - name: chatbot-ui
#           image: vikashmuktha/chatbot:latest
#           resources: {}
#           ports:
#             - containerPort: 3000
#           # env:
#           #   - name: OPENAI_API_KEY
#           #     valueFrom:
#           #       secretKeyRef:
#           #         name: chatbot-ui
#           #         key: OPENAI_API_KEY
# ---
# kind: Service
# apiVersion: v1
# metadata:
#   namespace: chatbot-ui
#   name: chatbot-ui
#   labels:
#     app: chatbot-ui
# spec:
#   ports:
#     - name: http
#       protocol: TCP
#       port: 80
#       targetPort: 3000
#   selector:
#     app: chatbot-ui
#   type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: chatbot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chatbot
  template:
    metadata:
      labels:
        app: chatbot
    spec:
      containers:
      - name: chatbot
        image: viakshmuktha/chatbot:latest
        ports:
        - containerPort: 3000   # Use port 3000

---
apiVersion: v1
kind: Service
metadata:
  name: chatbot-service
spec:
  selector:
    app: chatbot
  ports:
  - protocol: TCP
    port: 80                 # Expose port 80
    targetPort: 3000
  type: LoadBalancer
